{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "22"
  },
  "steps": {
    "install": {
      "commands": [
        { "cmd": "npm --prefix backend install" },
        { "cmd": "NODE_ENV=development npm --prefix frontend install" }
      ]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": [
        { "cmd": "NODE_ENV=production npm --prefix frontend run build" }
      ]
    }
  },
  "deploy": {
    "startCommand": "npm --prefix backend run start"
  }
}
