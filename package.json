{
  "name": "voxxstake",
  "version": "1.0.0",
  "description": "VOXX NFT Soft Staking Platform on Sui blockchain",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npx craco build",
    "start": "cd backend && npx tsx src/index.ts"
  },
  "engines": {
    "node": ">=22"
  }
}
