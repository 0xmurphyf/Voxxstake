{
  "name": "voxxstake",
  "version": "1.0.0",
  "description": "VOXX NFT Soft Staking Platform on Sui blockchain",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && yarn install",
    "build": "cd frontend && yarn build",
    "start": "cd backend && npx tsx src/index.ts"
  },
  "engines": {
    "node": ">=22"
  }
}
